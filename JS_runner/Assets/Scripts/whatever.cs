using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class whatever : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
       // Flow_C.instance.beforeEnd();

    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
